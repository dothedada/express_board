<!doctype html>
<html>
	<%- include("head", title)%>
    <body>
		<%- include("header") %> 
        <main>
			<% msgs.map((msg, i) => { %> 
				<%- include("card.ejs", {msg, id: i}) %>
			<% }) %>
        </main>
    </body>
</html>
